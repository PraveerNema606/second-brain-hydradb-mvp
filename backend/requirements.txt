slack_sdk>=3.27.0
python-dotenv>=1.0.0
requests>=2.31.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
openai>=1.30.0
apscheduler>=3.10.0
cachetools>=5.3.0
dateparser>=1.2.0

# --- Phase 1: Supabase multi-user auth ---
supabase>=2.4.0
PyJWT>=2.8.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0

# --- Phase C: Gmail attachment ingestion ---
# PDF text extraction. Optional at runtime: gmail_attachments guards the
# import and skips PDF attachments gracefully if it's unavailable. DOCX is
# handled with the stdlib (zipfile), CSV/TXT by decoding -- no extra deps.
pypdf>=4.0.0